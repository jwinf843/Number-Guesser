/// @description Initialize the Number Box
// You can write your code in this editor

/* This code doesn't work
answer = irandom(9) + 1
debug_message = "You rolled a " + string(answer)

show_debug_message(debug_message)
*/

// Apparently this function call makes it possible to actually generate a new number each time
randomize()

answer = irandom(9) + 1

